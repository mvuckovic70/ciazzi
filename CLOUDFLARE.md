# Cloudflare Pages — auto-detected, no config file needed.
# Settings to set in the CF Pages dashboard:
#   Framework preset: Astro
#   Build command:    npm run build
#   Build output dir: dist
#   Node version:     20

# _redirects (copy this file to public/_redirects):
# /sr/*   /:splat  301   — ako korisnik upiše /sr/ ručno, redirect na /
